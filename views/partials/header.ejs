<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <title><%= title %></title>
</head>
<body>
  <nav>
    <img src="/images/borrow-buddy-logo.png" width="auto" height="100px">
    <% if (user) { %>
        <a href="/items/new" <%- title === 'Add Item' ? 'class="active"' : '' %>>ADD ITEM</a>
        <a href="/logout">LOGOUT</a>
        <% } else { %>
            <a href="/auth/google" class="login">LOG IN&nbsp;<img src="https://i.imgur.com/FHjYyi0.png"</a>
            <a href="/items" <%- title === 'Items Inventory' ? 'class="active"' : '' %>>ITEM INVENTORY</a>
        <% } %>
  </nav>

  <nav>
    <img src="/images/borrow-buddy-logo.png" width="500px" height="auto">
    <a href="/items" <%- title === 'Item Inventory' ? 'class="active"' : '' %>>ITEM INVENTORY</a>
    <% if (user) { %>
      <a href="/items/new" <%- title === 'Add Item' ? 'class="active"' : '' %>>ADD ITEM</a>
      <a href="/logout">LOG OUT</a>
    <% } else { %>
      <a href="/auth/google" class="login">LOG IN&nbsp;<img src="https://i.imgur.com/FHjYyi0.png"></a>
    <% } %>
  </nav>



  <main>
    <h1><%= title %></h1>